# Distribution — handover

**Kept current, not written at the close.**

## Established

- **Fifteen of the ecosystem's twenty-eight roles sit in anoieu's tree.** Eight
  held directly, seven across its child projects. *Re-derive:* the *How many
  each holds* table in `roles.md` — `anoieu` 8, `ynoia` 5, `sapheneia` 1,
  `stathmos` 1, and three children at 0.
- **Twenty-one of twenty-two board items name anoieu.** From `S4` in martyria's
  stances, quoted in `history.md`.
- **`R28`, the epoch build role, sits with anoieu while `E1` is stuck.** `E1`
  has been `planned` for a whole term and its last dry run was `BLOCKED`. The
  role that moves it to `deployed` is held by the tree that is busiest.
- **The presidency was the first thing moved, and it was moved for this reason.**
- **`martyria` and `zetesis` are child projects of the repository whose conduct
  they assess.** anoieu's own register says so in those words. It is the `P2`
  defect — the judge inside the judged — in the ethics half, and nobody has
  raised it as a handoff.
- **Answering is slow, and that is measurable.** Three members declared
  membership at 10:53, 12:41 and 12:50; the inventory recorded all three at
  16:44. `D1` stayed open for an entire term. *Re-derive:* `history.md`'s
  entering table, and the topic dates in `discussion.md`.

### Also open

- **Is `R28` a president's role or a tool's?** `tekton` is reserved for the
  program; running a stretch by hand is what is actually happening, and nobody
  has said which of the two the office should be doing.
- **What is kanon holding that somebody else could hold?** Asked here because an
  office of distribution that never turns around is a complaints department.
  Today the honest answer is *nothing, it holds almost nothing at all*.

## Open

- **Who ends the ethics defect for good?** Settled: they come to kanon. Not
  settled: kanon is then the tree they sit in. `euboulia` is still the reserved
  destination and its stated blocker — advising a `nomophylax` that does not
  exist — is not a real one, since leaving the tree you assess does not depend
  on who you advise.
- **What is a good latency?** No target exists, so *slow* is currently an
  impression rather than a finding.

## Closed

- **Whether kanon builds its own communication machinery — no.** `koine` is the
  tool for it and is a member. Building a second implementation would break this
  office's own mission on its first task. *Do not reopen without new evidence.*
- **Whether `martyria` and `zetesis` come into `tools/kanon-ethics/` — yes.**
  *This entry said no for about an hour.* The objection stands on its facts and
  was answered rather than dismissed: an office does not travel, but what it
  holds does. **The half that is not answered is recorded as still true** —
  kanon holding the ecosystem's ethics as well as its own conduct relocates the
  judge-inside-the-judged defect rather than removing it, and this office should
  keep looking for the tool that ends it.

## In flight

- `B15` in anoieu — parked, scope being cut, and **not ours to start**.
- `K3` → koine, on what fast would take.
- `A13`, `A14` — the ethics-tree finding and the missing latency baseline.
