# Readiness — handover

**Kept current, not written at the close.**

## Established

- **kanon is `unknown`, which is not a pass.** This repository runs nothing.
  `PROTO-20` requires CI passing on both sides of a handoff with no exceptions
  and puts a repository that runs nothing with *attention* rather than *ok*.
  *Re-derive:* there is no workflow file in this tree.
- **anoieu is green.** Ten consecutive successful `ci` runs on 2026-09-02,
  ending a red streak of 112. *Re-derive:* the public run history.
- **The stub in anoieu should stay**, and this office says so rather than
  waiting to be asked.

## Open

- **What is the smallest thing kanon can run that actually fails when something
  is wrong?** A check that cannot go red buys the colour and not the property.

## Closed

Nothing yet.

## In flight

- `A5` — the only item on the list kanon can do without asking anybody.
